{
  "name": "team-task-manager-root",
  "version": "1.0.0",
  "description": "Root package.json to facilitate seamless Railway deployment for the backend",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
